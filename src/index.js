import { openTaskModal } from "../js_modules/openTaskModal";
import { addTaskInfo } from "../js_modules/addTaskInfo";
import { switchTabs } from "../js_modules/switchTabs";

switchTabs();
openTaskModal();
addTaskInfo();


// I need to retrieve date from form
// I need to retrieve current date from the current user's computer
// I need to compare the date from the form and the current date
// If the current dates match, I need to load the task in today tab
// else if the date is in the future, I need to load the task in upcoming tab

